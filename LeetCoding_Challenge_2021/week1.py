# Max Area of Island


class Solution:
	
    def maxAreaOfIsland(self, grid: List[List[int]]) -> int:
        for y in range(len(grid)):
			for x in range(len(grid[y])):
				if grid[y][x] == 1:
					pass

